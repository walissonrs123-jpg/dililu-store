# Segurança
HTTPS obrigatório; S3 privado; CloudFront OAC; AWS via OIDC; sem access keys no repo; sem tokens Meta; sem dados pessoais persistidos; sem painel interno exposto; CSP/headers de segurança; `content/` e `docs/` não são rotas públicas.
